<div dir="rtl">

# 🛠 راهنمای فنی — انتشار و مشارکت

> این سند، راهنمای عملیاتی پروژهٔ **[مرجع فارسی Hermes Agent](index.html)** است: چطور سایت را روی GitHub Pages یا Cloudflare Pages منتشر کنید و چطور صفحهٔ ترجمهٔ جدید اضافه کنید.

---

## ۱) انتشار روی GitHub Pages

۱. یک مخزن جدید بسازید (مثلاً `hermes-docs-fa`).

۲. محتوای این پوشه را در آن push کنید:

```bash
git init
git add .
git commit -m "مرجع فارسی مستندات Hermes Agent"
git branch -M main
git remote add origin https://github.com/<username>/hermes-docs-fa.git
git push -u origin main
```

۳. در تنظیمات مخزن: **Settings ← Pages ← Build and deployment** و گزینهٔ **Deploy from a branch**، شاخهٔ `main` و پوشهٔ `/ (root)` را انتخاب کنید.

۴. پس از چند دقیقه سایت روی `https://<username>.github.io/hermes-docs-fa/` در دسترس است.

> 💡 نکته: پوشهٔ `_source/` (متن اصلی دانلودشده برای ترجمه) را می‌توانید در `.gitignore` بگذارید یا هنگام انتشار حذفش کنید — فقط برای فرایند ترجمه لازم است.

## ۲) انتشار روی Cloudflare Pages

۱. وارد داشبورد Cloudflare Pages شوید و **Create a project → Direct Upload** را بزنید (یا مخزن گیت را وصل کنید).

۲. پوشهٔ `hermes-docs-fa` را آپلود کنید — بدون هیچ تنظیم بیلد:
   - **Framework preset:** `None`
   - **Build command:** (خالی)
   - **Output directory:** `/`

۳. دامنهٔ اختصاصی هم از همان‌جا قابل اتصال است.

## ۳) روند افزودن ترجمهٔ جدید (برای مشارکت‌کنندگان)

۱. متن اصلی صفحه را از `_source/pages/` بردارید (تفکیک‌شده از `llms-full.txt` رسمی).

۲. یک فایل HTML با قالب صفحات موجود بسازید — ساده‌ترین راه، کپی یکی از صفحات فعلی و جایگزینی محتواست. فقط این‌ها را تنظیم کنید:
   - `data-root` در تگ `<html>` (عمق نسبی نسبت به ریشه: `..` یا `../..`)
   - `<title>`، بردکرامب، لینک «نسخهٔ اصلی» و فوتر
   - لینک‌های قبلی/بعدی در `.pager`

۳. در `assets/nav-data.js` پرچم `d` آن صفحه را از `0` به `1` تغییر دهید — سایدبار همهٔ صفحات، کاتالوگ صفحهٔ اصلی و نوار پیشرفت به‌طور خودکار به‌روز می‌شوند.

### اصول ترجمه

وفاداری کامل به متن اصلی (هیچ بخشی حذف یا خلاصه نمی‌شود) · فارسی روان و ساده · دستورها و کدها بدون هیچ تغییری · لینک صفحات ترجمه‌نشده به نسخهٔ انگلیسی رسمی.

> 📋 جزئیات کامل‌تر (چک‌لیست ۷ مرحله‌ای، واژه‌نامهٔ مصطلحات، اولویت‌ها) در [WORKLOG.md](WORKLOG.md) آمده است.

</div>
